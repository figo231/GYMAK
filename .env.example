# Copy this file to .env and fill in your Supabase project's values
# (Project Settings -> API in the Supabase dashboard).
# The app runs fully offline on localStorage without these set —
# they're only required once Phase 3 (auth + cloud sync) is wired in.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
